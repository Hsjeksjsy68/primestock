--- src/App.tsx
+++ src/App.tsx
@@ -29,6 +29,7 @@
 import AdminDashboard from "./AdminDashboard";
 import GiftCards from "./GiftCards";
 import SellerAdsTab from "./SellerAdsTab";
+import SellerSettingsTab from "./SellerSettingsTab";
 
 export default function App() {
@@ -1462,6 +1463,12 @@
                   >
                     Run Ads
                   </button>
+                  <button
+                    onClick={() => setActiveTab("settings")}
+                    className={`py-4 px-6 font-black uppercase tracking-widest text-sm border-b-2 transition-colors ${activeTab === "settings" ? "border-[#D4FF00] text-white" : "border-transparent text-neutral-500 hover:text-neutral-300"}`}
+                  >
+                    Settings
+                  </button>
                 </div>
 
                 <div className="p-6 sm:p-8">
@@ -1787,6 +1794,9 @@
                   {activeTab === "ads" && (
                     <SellerAdsTab user={user} />
                   )}
+                  {activeTab === "settings" && (
+                    <SellerSettingsTab user={user} sellerProfile={sellerProfile} />
+                  )}
                 </div>
               </div>
